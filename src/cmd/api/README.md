# Weather API

The Weather API is a simple REST server that exposes current and historical weather data from the database. It provides endpoints to list supported cities, get current weather for a city, and query historical weather over a time range.
